package com.jt;


import java.util.HashMap;
import java.util.Map;

public class DrinkVendingMachine implements VendingMachine {

    @Override
    public HashMap<Integer, Integer> calculateChangeCoins(int moneyToReturn, HashMap<Integer, Integer> moneyStock) {
        HashMap<Integer, Integer> calculatedChange = new HashMap<>();

        return calculatedChange;
    }
}
